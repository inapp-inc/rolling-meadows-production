# Case Management Platform — local Docker stack (React + FastAPI + PostgreSQL)

services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: case_management
      POSTGRES_PASSWORD: case_management
      POSTGRES_DB: case_management
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U case_management -d case_management"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://case_management:case_management@postgres:5432/case_management
      API_ENABLED_MODULES: health,auth,admin,platform,clients,catalog,cases,enrollments,liaison,workflow,documents,reports
      JWT_SECRET: ${JWT_SECRET:-dev-change-me-in-production}
      JWT_EXPIRES_MINUTES: ${JWT_EXPIRES_MINUTES:-480}
      CORS_ORIGINS: http://localhost:8081,http://localhost:5173
      SEED_USER_PASSWORD: ${SEED_USER_PASSWORD:-ChangeMe123!}
      DEFAULT_TENANT_ID: tenant-demo
    volumes:
      - branding_data:/app/branding
    depends_on:
      postgres:
        condition: service_healthy

  web:
    build:
      context: ./web
      args:
        VITE_API_BASE_URL: "/api"
        VITE_USE_MOCK_AUTH: "false"
    ports:
      - "8081:80"
    depends_on:
      - api

volumes:
  postgres_data:
  branding_data:
